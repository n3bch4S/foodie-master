import { Dom } from "./types";

export const HOME_PAGE_DOM: Dom = { id: "root", innerText: "", children: [] };
export const ORDER_PAGE_DOM: Dom = { id: "root", innerText: "", children: [] };
export const CUSTOM_PAGE_DOM: Dom = { id: "root", innerText: "", children: [] };
